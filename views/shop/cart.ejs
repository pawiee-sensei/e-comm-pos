<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/shop.css">
</head>
<body>

<div class="container py-4">
  <h3>Your Cart</h3>

  <% if (items.length === 0) { %>
    <p class="text-muted">Your cart is empty.</p>
  <% } else { %>

    <table class="table">
      <thead>
        <tr>
          <th>Product</th>
          <th>Qty</th>
          <th>Price</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% items.forEach(i => { %>
          <tr>
            <td><%= i.name %></td>
            <td><%= i.qty %></td>
            <td>₱<%= i.price * i.qty %></td>
            <td>
              <form method="POST" action="/cart/remove/<%= i.id %>">
                <button class="btn btn-sm btn-outline-danger">Remove</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

  <% } %>

  <a href="/shop" class="btn btn-outline-dark btn-sm">
    Continue Shopping
  </a>
</div>

</body>
</html>
