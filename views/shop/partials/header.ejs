<header class="shop-header">
  <div class="container d-flex justify-content-between align-items-center py-3">
    <h4 class="mb-0">
      <a href="/shop" class="text-decoration-none text-dark">
        Coffee Shop
      </a>
    </h4>

    <div class="shop-actions d-flex align-items-center gap-3">
      <a href="/cart" class="text-decoration-none">
        🛒 Cart
      </a>

      <% if (session && session.user) { %>
        <span class="text-muted small">
          Hi, <%= session.user.name %>
        </span>
        <a href="/logout" class="btn btn-outline-dark btn-sm">Logout</a>
      <% } else { %>
        <a href="/login" class="btn btn-outline-dark btn-sm">Login</a>
      <% } %>
    </div>
  </div>
</header>
